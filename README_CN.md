# 基于运动学仿真与优化算法的烟幕干扰弹投放策略

[![C++ Standard](https://img.shields.io/badge/C%2B%2B-17-blue.svg)](https://isocpp.org/)
[![Build System](https://img.shields.io/badge/Build-CMake-orange.svg)](https://cmake.org/)
[![License: MIT](https://img.shields.io/badge/License-MIT-yellow.svg)](https://opensource.org/licenses/MIT)

🏆 **荣获 2025 年高教社杯全国大学生数学建模竞赛（安徽赛区）二等奖** (中国工业与应用数学学会 CSIAM)

**[English Version](./README.md)** | [中文版]

>   **说明**: 本仓库包含了我们团队参与全国大学生数学建模竞赛的核心 C++ 寻优引擎与 Python 可视化代码。
>
>   **💡 核心要点**: 关于严谨的数学建模推导、运动学方程及算法收敛性证明，请查阅我们的 **[赛题解决方案报告 (PDF)](./docs/Report.pdf)**。

## 📖 项目简介

本项目为**多无人机烟幕干扰弹投放策略**提供了一个高性能的 C++ 优化引擎。系统基于高精度的 **0.1ms 离散时间步长仿真**，精确建立了抛体运动、匀速下沉及圆柱体动态视觉遮蔽的几何模型。项目循序渐进地解决了 5 个层级的战术问题，最终跨越到极其复杂的 **40维多机协同、多目标拦截的最优解求解**，能够直接输出具备高度实操价值的无人机作战指令（速度、航向、投放时机等）。

![](./docs/images/strategy-visualizer-sample.png)

> **3D 战术可视化**：展示了 5 架无人机对抗 3 枚导弹的拦截策略。蓝色实线为导弹轨迹，橙色虚线为无人机航线，紫色三角代表烟幕起爆点。

## ✨ 算法亮点与创新

基于严密的数学建模，本引擎针对不同维度的优化需求设计了专属算法，有效克服了高维空间的“维度灾难”：

-   **1维运动学仿真 (问题一):** 采用 0.1ms 步长的数值仿真，精确建立目标视线到圆柱体轮廓边界的动态几何判定模型。计算有效遮蔽时长：*1.392s*。
-   **4维混合随机优化 (问题二):** 设计**两阶段混合随机优化算法**（大规模蒙特卡洛全局探索 + 多轮迭代式局部细化），解决单机单弹的高度非线性寻优。计算有效遮蔽时长：*4.589s*。
-   **8维差分进化 (问题三):** 引入**差分进化算法**，通过“变异-交叉-选择”机制在复杂的八维解空间中高效搜索，并采用区间并集逻辑完美解决多个烟幕云团遮蔽时间的重叠累加问题。计算有效遮蔽时长：*7.6s*。
-   **40维协同进化 (问题四):** 独创**协同进化框架**。将高维问题分解为多个独立的子问题并维护独立种群，采用“协同适应度评估”机制成功解决多机联动决策难题。
-   **时空解耦与网格搜索 (问题五):** 面对多机多弹场景，采用“问题解耦-子问题寻优-全局整合”的分层策略。通过时空关联性分析拆分独立的“烟幕弹-导弹”子空间，结合**网格搜索**与**局部爬山法 ** 突破计算瓶颈。

## 🛠 工程重构说明 

本项目对竞赛原始提交版代码进行了深度重构，其核心改进在于实现了**物理仿真模型与寻优算法的模块化解耦**。通过建立这一抽象层，用户无需触动底层动力学逻辑，即可通过配置文件灵活切换作战环境参数或调整优化策略。这一转变使本项目从单一的“赛题解法”升华为一套**高复用性的通用战术仿真引擎**。

## 🏗 工程架构

```
UAV-Kinematic-Optimizer/
├── config/
│   ├── config.h           # [环境与物理] 物理常数与赛题预设参数（如重力加速度、导弹速度、仿真步长等）
│   └── params.h           # [算法超参数] 启发式调参与寻优空间定义（如种群规模、变异率、搜索边界等）
├── core/  
│   ├── problem1.cpp       # 基于 0.1ms 高精度离散时间步进的运动学仿真（基础模型）
│   ├── problem2.cpp       # 4维参数寻优：两阶段混合随机搜索算法实现
│   ├── problem3.cpp       # 8维参数寻优：差分进化算法实现
│   ├── problem4.cpp       # 12维参数寻优：多机协同进化框架实现
│   ├── problem5-grid-search.cpp   # 40维策略 A：基于时空解耦的网格搜索实现
│   ├── problem5-hill-climbing.cpp # 40维策略 B：启发式局部爬山寻优实现
│   └── problem5-simulator.cpp     # 40维策略验证：多机多弹动力学模拟器
├── data/   
│   ├── problem5-grid-search-strategy.csv   # 格式：飞机 ID + 3列导弹拦截顺序 + 2维基础参数（速度与航向）
│   ├── problem5-hill-climbing-strategy.csv # 格式：飞机 ID + 3列导弹拦截顺序 + 8维完整参数（含多行备用样例）
│   ├── problem5-simulator-strategy.csv     # 格式：飞机 ID + 8维完整参数（直接映射至程序仿真数组）
│   └── results.xlsx       # 最终汇总表 (包含子问题 1-3 的寻优坐标与遮蔽效能)
├── docs/
│   ├── Problem_A.pdf      # 原始赛题文件（物理约束与任务目标的来源）
│   ├── Report.pdf         # 赛题解决方案报告（核心数学建模与算法证明）
│   └── images/            # 存放 README 使用的配图（架构图、仿真截图等）
├── viz/         
│   ├── requirements.txt   # Python 环境依赖清单（Numpy, Matplotlib 等）
│   └── strategy-visualizer.py # 核心 3D 轨迹渲染与遮蔽效果动态展示脚本
├── .gitignore             # 忽略编译产物与临时日志，保持仓库整洁
├── CMakeLists.txt         # 跨平台构建系统：实现全模块一键编译
├── README.md              # 项目文档（英文版）
└── README_CN.md           # 项目文档（中文版）
```

## 📊 数据输入格式说明

为实现模块解耦，本引擎依赖 CSV 文件进行策略的传递与验证。核心任务参数映射矩阵 (`final_params[i]`) 格式如下：

| **数组索引** | **变量名**          | **物理含义**                        | **单位**   |
| ------------ | ------------------- | ----------------------------------- | ---------- |
| `[ID]`       | `uav_id`            | 飞机编号 (1-5, 对应 FY1-FY5)        | -          |
| `[0]`        | `uav_speed`         | 无人机水平飞行速度                  | m/s        |
| `[1]`        | `uav_fly_angle_rad` | 无人机飞行航向角                    | rad (弧度) |
| `[2]`        | `t_drop1`           | 第 1 枚烟雾弹的绝对投放时间         | s          |
| `[3]`        | `delay1`            | 第 1 枚烟雾弹从投放到爆炸的延迟时间 | s          |
| `[4]`        | `delta_t_drop2`     | 第 2 枚与第 1 枚投放的时间间隔 (Δt) | s          |
| `[5]`        | `delay2`            | 第 2 枚烟雾弹的引信延迟时间         | s          |
| `[6]`        | `delta_t_drop3`     | 第 3 枚与第 2 枚投放的时间间隔 (Δt) | s          |
| `[7]`        | `delay3`            | 第 3 枚烟雾弹的引信延迟时间         | s          |

1.  **`problem5-simulator-strategy.csv`**：直接按 `ID列 + 上述 8 个参数` 的标准格式存储。
2.  **`problem5-hill-climbing-strategy.csv`**：在 `ID` 列之后增加了三列导弹顺序标识（如 M2, M1, M3），其后跟随上述 8 个参数。*(注：代码在读取时仅将第一个有效的编队配置行作为实际输入，其余行仅作为更多样式的备用例提供)*。
3.  **`problem5-grid-search-strategy.csv`**：包含 `ID` 列、三列导弹顺序标识，以及最基础的速度与航向角 2 个参数。*(同样仅读取首个有效行)*。

## 🚀 快速启动

**1. 环境准备 (UCRT64)**

请确保您的系统已安装 GCC 17+（需支持 `std::filesystem` 及 `inline` 变量）与 CMake 3.10+。

**2. 编译寻优引擎**

```bash
mkdir build && cd build
cmake -G "Unix Makefiles" ..
make -j4
```

**3. 生成战术策略**

请在终端中运行生成的可执行文件。**程序的迭代收敛日志以及最终的最优策略结果（包含上述所有速度、时间变量）将直接打印在终端中**。建议使用重定向符号 `>` 保存运行日志：

```bash
cd ../bin
./problem4.exe > output_log.txt
```

**4. 3D 战场环境可视化**

```bash
pip install -r viz/requirements.txt
python viz/strategy-visualizer.py
```

## 👥 团队成员

-   **奚项正** (中国科学技术大学 少年班学院)
-   **赵一帆** (中国科学技术大学 少年班学院)